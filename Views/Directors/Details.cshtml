@model MvcMovie.Models.Director

@{ ViewData["Title"] = "Details"; }
<style>

    .row {
        background-color: whitesmoke;
        border-radius: 20px;
        color: black;
    }
</style>
<h1>Details</h1>

<div>
    <h4>Director</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>

<table class="table">
    <thead>
        <tr>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Movies)
        {
<tr>
    <td>
        @item.Title
    </td>
    <td>
        <a class="btn btn-small btn-danger" asp-action="removeMovDir" asp-route-id="@item.ID">Delete</a> |

    </td>
</tr>}
    </tbody>
</table>

<div>

    <a class="btn btn-small btn-success" asp-action="Edit" asp-route-id="@Model.ID">Edit</a> |
    <a class="btn btn-small btn-secondary" asp-action="Index">Back to List</a>
</div>
